array are not immutable their size is immutable 
for example arr = {1,2,3,4,5};
and arr.length = 5;
then we replace the arr elements but not add any element because adding arr elements will increase the size

if question of leetcode is about subarray than use 2 pointer approach
